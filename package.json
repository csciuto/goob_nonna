{
  "name": "goob-nonna",
  "version": "1.0.0",
  "description": "Web-based Moog Grandmother synthesizer clone",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "serve": "npx serve . -p 8080"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "@playwright/test": "^1.44.0"
  }
}
